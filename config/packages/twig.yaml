twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes: ['bootstrap_4_layout.html.twig'] # accepte plusieurs thèmes
    globals:
        app_name: 'Malick Tounkara'
when@test:
    twig:
        strict_variables: true
